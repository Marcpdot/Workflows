# Moved: packages/orchestrator

The runnable Orchestrator package now lives at:

**[`packages/orchestrator`](../packages/orchestrator/README.md)**

```bash
cd packages/orchestrator
npm install
npm run dev -- "your prompt"
```

This folder is kept only as a path pointer for old links (Step F of `docs/PACKAGE_REFACTOR.md`).
