# Eval cases moved

Cases and implementation live under the repo-root package:

**`packages/eval/cases.json`**  
**`packages/eval/src/`**

Run from Orchestrator (unchanged):

```bash
npx tsx scripts/run-eval.ts
```

See `docs/PACKAGE_REFACTOR.md` Step A and `packages/eval/README.md`.
