{
  "name": "@workflows/surface",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Work surface HTTP client — one room over the orchestrator integration server",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "awake": "node scripts/awake.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "vite": "^6.3.5"
  }
}
